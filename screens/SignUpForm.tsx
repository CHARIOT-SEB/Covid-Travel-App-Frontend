import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const SignUpForm = () => {
  return (
    <View>
      <Text>This is the SignUpForm</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});


export default SignUpForm;